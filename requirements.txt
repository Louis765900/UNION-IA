# Cœur (requis)
numpy>=1.21.0
rich>=12.0.0
flask>=2.3.0

# Moteurs LLM cloud
openai>=1.0.0              # DeepSeek API (compatible OpenAI)
google-generativeai>=0.8.0 # Gemini API

# Skills — fichiers riches (optionnels, installés à la demande)
pypdf>=4.0           # Lecture PDF (léger)
pdfplumber>=0.10     # PDF avec tableaux (plus robuste)
openpyxl>=3.1        # Excel .xlsx
python-docx>=1.1     # Word .docx
odfpy>=1.4           # LibreOffice .odt

# LLM local (fallback, nécessite GPU + fichiers GGUF)
# llama-cpp-python>=0.3.0
